# firefly 仓库规则

## 自身规划边界

- 本仓库是工作流分发与覆盖仓库，不把仓库自身作为 firefly/OpenSpec 产品项目递归管理。
- 根目录的 `openspec/`、`docs/plans/`、`.firefly/` 和 `.superpowers/` 仅可作为本地临时规划或运行状态，必须保持忽略且不得提交。
- 不得使用 `git add -f` 强制提交上述自身规划或运行产物；已被跟踪的同类文件必须从当前版本清退。
- `assets/openspec/` 是向用户分发的产品资产，不属于仓库自身规划产物，必须继续跟踪维护。
- 设计 firefly 自身变更时，直接修改受管资产、脚本和测试；本地方案可留在被忽略目录，但不得进入发布资产或 Git 提交。

## 平台能力基线

- 只设计和维护 Codex 与 Claude Code 共同支持的能力交集。
- Codex 与 Claude Code 均支持的能力，默认视为其他兼容平台也支持，不增加“平台不支持子代理”等降级分支。
- 自治工作流默认具备独立子代理能力；执行代理与审查代理必须分离。
- 平台主代理是唯一流程控制器；不得在 firefly 内重复实现状态内核、锁、租约或跨进程调度器。
- 每个工作树只运行一个主流程，进度恢复复用 `tasks.md`、Git 和现有 SDD progress。

## 受管目录的符号链接

- 用户可用符号链接管理 `~/.codex`、`~/.claude` 及其受管子目录的实际存储位置，例如迁移到另一块磁盘；这是合法的 Linux 存储拓扑。
- 安装器与 doctor/repair 必须遵循用户定义的符号链接并在其解析后的目标中工作，不得将其视为不安全输入，也不得删除、替换或拆开这些链接。
